if SHIELD_NANO18_LEFT

config ZMK_KEYBOARD_NAME
    default "nano18"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_NANO18_LEFT || SHIELD_NANO18_RIGHT

config ZMK_SPLIT
    default y

endif
